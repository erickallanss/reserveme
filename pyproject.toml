[project]
name = "reserveme"
version = "0.1.0"
description = "ReserveMe - Sistema de reservas"
requires-python = ">=3.11"
dependencies = [
    "django>=5.0,<6.0",
    "psycopg2-binary>=2.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
